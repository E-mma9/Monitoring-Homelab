groups:
  - name: node_alerts
    rules:
      - alert: NodeDown
        expr: up{job="proxmox-nodes"} == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Node {{ $labels.instance }} is down"
          description: "{{ $labels.instance }} has been unreachable for more than 1 minute."

      - alert: DiskSpaceWarning
        expr: 100 - (node_filesystem_avail_bytes{mountpoint="/"} / node_filesystem_size_bytes{mountpoint="/"} * 100) > 85
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "Disk almost full on {{ $labels.instance }}"
          description: "Root partition on {{ $labels.instance }} is {{ $value | printf \"%.1f\" }}% full."

      - alert: DiskSpaceCritical
        expr: 100 - (node_filesystem_avail_bytes{mountpoint="/"} / node_filesystem_size_bytes{mountpoint="/"} * 100) > 95
        for: 2m
        labels:
          severity: critical
        annotations:
          summary: "Disk CRITICALLY full on {{ $labels.instance }}"
          description: "Root partition on {{ $labels.instance }} is {{ $value | printf \"%.1f\" }}% full. Action required!"

      - alert: HighMemoryUsage
        expr: 100 * (1 - (node_memory_MemAvailable_bytes / node_memory_MemTotal_bytes)) > 90
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "High RAM usage on {{ $labels.instance }}"
          description: "{{ $labels.instance }} is using {{ $value | printf \"%.1f\" }}% of RAM."

      - alert: HighCPUUsage
        expr: 100 - (avg by(instance) (rate(node_cpu_seconds_total{mode="idle"}[5m])) * 100) > 95
        for: 10m
        labels:
          severity: warning
        annotations:
          summary: "High CPU usage on {{ $labels.instance }}"
          description: "{{ $labels.instance }} CPU is at {{ $value | printf \"%.1f\" }}% for more than 10 minutes."

  - name: service_alerts
    rules:
      - alert: ServiceDown
        expr: monitor_status{} == 0
        for: 2m
        labels:
          severity: critical
        annotations:
          summary: "Service {{ $labels.monitor_name }} is down"
          description: "Uptime Kuma reports that {{ $labels.monitor_name }} has been down for more than 2 minutes."
